# Ainekio Site

Astro/Svelte static site for `ainek.io`, based on the existing Merkin site template.

## Commands

From the repo root:

```bash
pnpm install
pnpm build
pnpm dev
```

The GitHub Pages workflow builds `apps/site` and deploys `apps/site/dist`.
